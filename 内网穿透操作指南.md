# FastAPI + Vue 项目内网穿透操作指南

这份指南针对当前项目：

```text
D:\desktop\agent\FastAPI\news_project
```

项目结构大致是：

```text
backend   FastAPI 后端，默认端口 8000
frontend  Vue/Vite 前端，默认端口 5173
MySQL     Docker 容器，只给后端访问，不需要暴露给公网
```

目标是：给朋友一个公网链接，让他能打开完整前端页面，并且页面能请求你的后端接口。

## 一、核心原理

朋友访问完整页面时，链路是：

```text
朋友浏览器
  ↓
前端 trycloudflare 链接
  ↓
你的 Vite 前端 5173
  ↓
前端代码请求后端 trycloudflare 链接
  ↓
你的 FastAPI 后端 8000
  ↓
你的 Docker MySQL 3306
```

所以需要两个 tunnel：

```text
后端 tunnel：把公网地址转发到本机 8000
前端 tunnel：把公网地址转发到本机 5173
```

MySQL 不需要 tunnel，也不应该暴露到公网。

## 二、推荐启动顺序

建议每次按这个顺序来：

```text
1. 启动 MySQL Docker
2. 启动 FastAPI 后端
3. 启动后端 cloudflared tunnel
4. 把前端 baseURL 改成后端 tunnel 地址
5. 启动 Vue/Vite 前端
6. 启动前端 cloudflared tunnel
7. 把前端 tunnel 地址发给朋友
```

## 三、启动 MySQL

如果 MySQL 容器已经存在，直接启动：

```powershell
docker start mysql_fastapi
```

查看是否运行：

```powershell
docker ps
```

看到类似下面内容就说明 MySQL 在跑：

```text
mysql_fastapi   mysql   0.0.0.0:3306->3306/tcp
```

注意：不要给 MySQL 做 cloudflared tunnel。

## 四、启动 FastAPI 后端

在 PowerShell 里运行：

```powershell
cd D:\desktop\agent\FastAPI\news_project
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000
```

检查后端是否正常：

```powershell
curl http://127.0.0.1:8000/docs
```

浏览器也可以打开：

```text
http://127.0.0.1:8000/docs
```

如果打不开，先不要开 tunnel，先解决后端本地启动问题。

## 五、启动后端 tunnel

新开一个 PowerShell 窗口，运行：

```powershell
docker run --rm cloudflare/cloudflared:latest tunnel --protocol http2 --url http://host.docker.internal:8000
```

成功后会输出类似：

```text
Your quick Tunnel has been created!
https://xxxx-yyyy-zzzz.trycloudflare.com
```

这个是后端公网地址。

你可以测试：

```text
https://xxxx-yyyy-zzzz.trycloudflare.com/docs
```

如果能打开 FastAPI 文档，后端 tunnel 就成功了。

## 六、修改前端后端地址

打开：

```text
D:\desktop\agent\FastAPI\news_project\frontend\src\config\api.js
```

把：

```js
baseURL: 'http://127.0.0.1:8000'
```

改成刚才后端 tunnel 给你的地址，例如：

```js
baseURL: 'https://xxxx-yyyy-zzzz.trycloudflare.com'
```

注意：这里不能写：

```js
http://127.0.0.1:8000
```

也不能写：

```js
http://host.docker.internal:8000
```

原因是：前端代码运行在朋友的浏览器里。朋友浏览器里的 `127.0.0.1` 指的是朋友自己的电脑，不是你的电脑。

## 七、启动 Vue/Vite 前端

新开一个 PowerShell 窗口，运行：

```powershell
cd D:\desktop\agent\FastAPI\news_project\frontend
npm.cmd run dev -- --host 0.0.0.0 --port 5173
```

检查前端是否正常：

```text
http://127.0.0.1:5173
```

如果这个本地地址打不开，前端 tunnel 一定也打不开。

## 八、启动前端 tunnel

新开一个 PowerShell 窗口，运行：

```powershell
docker run --rm cloudflare/cloudflared:latest tunnel --protocol http2 --url http://host.docker.internal:5173 --http-host-header localhost:5173
```

成功后会输出类似：

```text
https://aaaa-bbbb-cccc.trycloudflare.com
```

这个才是发给朋友的完整前端页面地址。

## 九、为什么前端 tunnel 要加 http-host-header

关键参数是：

```powershell
--http-host-header localhost:5173
```

原因是 Vite 有 Host 校验。

如果 cloudflared 直接把请求转给 Vite，Vite 看到的 Host 会是：

```text
xxxx.trycloudflare.com
```

Vite 可能会拒绝请求，并返回：

```text
403 Forbidden
Blocked request. This host is not allowed.
```

加上：

```powershell
--http-host-header localhost:5173
```

意思是告诉 cloudflared：访问本地 Vite 时，把 Host 伪装成 `localhost:5173`。Vite 允许 localhost，所以页面就能正常打开。

## 十、每个参数是什么意思

后端 tunnel：

```powershell
docker run --rm cloudflare/cloudflared:latest tunnel --protocol http2 --url http://host.docker.internal:8000
```

解释：

```text
docker run
```

启动一个 Docker 容器。

```text
--rm
```

容器停止后自动删除，不留临时容器。

```text
cloudflare/cloudflared:latest
```

使用 Cloudflare 官方 cloudflared 镜像。

```text
tunnel
```

启动 Cloudflare Tunnel 功能。

```text
--protocol http2
```

让 cloudflared 用 HTTP/2 连接 Cloudflare。你之前 VPN/代理环境下 QUIC 不稳定，所以建议保留这个参数。

```text
--url http://host.docker.internal:8000
```

告诉 cloudflared：公网请求要转发到你电脑上的后端 8000 端口。

在 Docker 容器里，`127.0.0.1` 指的是容器自己，不是你的电脑。所以这里用：

```text
host.docker.internal
```

前端 tunnel：

```powershell
docker run --rm cloudflare/cloudflared:latest tunnel --protocol http2 --url http://host.docker.internal:5173 --http-host-header localhost:5173
```

多出来的：

```text
--http-host-header localhost:5173
```

是为了绕过 Vite 的 Host 校验。

## 十一、常见错误排查

### 1. Cloudflare 页面显示 502 Bad Gateway

常见原因：

```text
cloudflared 能连到 Cloudflare，但连不到你本地服务
```

检查本地服务：

```powershell
curl http://127.0.0.1:5173
curl http://127.0.0.1:8000/docs
```

检查 Docker 容器能不能访问本机服务：

```powershell
docker run --rm curlimages/curl:latest -I http://host.docker.internal:5173
docker run --rm curlimages/curl:latest -I http://host.docker.internal:8000/docs
```

如果前端返回 `403 Forbidden`，说明是 Vite Host 校验问题，前端 tunnel 要加：

```powershell
--http-host-header localhost:5173
```

### 2. Cloudflare 页面显示 1033

常见原因：

```text
cloudflared tunnel 没有正常连到 Cloudflare
```

可能是：

```text
cloudflared 容器停了
终端关了
VPN/代理影响了 Docker 网络
Cloudflare quick tunnel 临时失效
```

检查：

```powershell
docker ps
```

如果看不到 cloudflared 容器，就重新运行 tunnel 命令。

### 3. 前端页面能打开，但接口没数据

检查前端配置：

```text
frontend\src\config\api.js
```

里面的 `baseURL` 必须是当前最新的后端 tunnel 地址。

然后测试后端接口：

```text
https://后端地址.trycloudflare.com/api/news/categories
```

如果这个接口打不开，说明后端 tunnel 或后端服务有问题。

### 4. 开了 VPN 后 tunnel 不稳定

你之前遇到过 cloudflared 日志里出现：

```text
Failed to dial a quic connection
DialContext error
```

处理方式：

```text
关闭 VPN 后重试
保留 --protocol http2
或者改用 Windows 原生 cloudflared.exe
```

## 十二、最终给朋友哪个链接

不要发后端地址：

```text
https://后端地址.trycloudflare.com/docs
```

这个只是 API 文档。

应该发前端地址：

```text
https://前端地址.trycloudflare.com
```

后端地址只写在前端配置里，给前端代码请求接口用。

## 十三、窗口不要关

演示期间至少保持这些东西运行：

```text
1. MySQL Docker
2. FastAPI 后端窗口
3. 后端 cloudflared tunnel 窗口
4. Vue/Vite 前端窗口
5. 前端 cloudflared tunnel 窗口
```

如果使用 `docker run -d` 后台运行 tunnel，可以用下面命令查看：

```powershell
docker ps
```

停止某个 tunnel：

```powershell
docker stop 容器名
```

## 十四、一套完整命令模板

后端：

```powershell
cd D:\desktop\agent\FastAPI\news_project
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000
```

后端 tunnel：

```powershell
docker run --rm cloudflare/cloudflared:latest tunnel --protocol http2 --url http://host.docker.internal:8000
```

前端：

```powershell
cd D:\desktop\agent\FastAPI\news_project\frontend
npm.cmd run dev -- --host 0.0.0.0 --port 5173
```

前端 tunnel：

```powershell
docker run --rm cloudflare/cloudflared:latest tunnel --protocol http2 --url http://host.docker.internal:5173 --http-host-header localhost:5173
```

记住：

```text
后端 tunnel 地址：写进 frontend\src\config\api.js
前端 tunnel 地址：发给朋友访问
```
